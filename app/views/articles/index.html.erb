<h1>Articles <span class='text-sm px-1 py-0.5 bg-red-300 rounded-md'><%= Article.public_count %></span></h1>

<% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article %>
  </li>
<% end %>

<%= link_to "New Article", new_article_path, class: 'bg-blue-700 text-white p-2' %>